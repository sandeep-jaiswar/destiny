/**
 * Terminal home page.
 * The layout.tsx provides the entire Bloomberg-style interface.
 * This page is intentionally minimal — all content comes from the layout.
 */

export default function TerminalPage() {
  return null;
}
